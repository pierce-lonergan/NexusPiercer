{
  "_comment": [
    "Static-analysis debt ceilings. These may only ever DECREASE.",
    "",
    "Checkstyle, PMD and SpotBugs had never executed against this codebase before 2026-08-09 —",
    "the audit proved it by observing that none of the three plugins had ever been downloaded to",
    "the local repository. Turning them on produced 700 violations at once, which is far too many",
    "to fix in one change and far too many to justify leaving ungated.",
    "",
    "A ratchet resolves that. The gate does not demand zero; it demands 'no worse than today'.",
    "New debt is blocked from the first run, and every fix lowers the ceiling permanently. This is",
    "the same mechanism already used for line coverage (jacoco.minimum.coverage). It was also used",
    "for dynamic dispatch (benchmarks/results/indy-ceiling.txt); that one was retired on",
    "2026-08-11 because it counted invokedynamic as a proxy for Groovy, and with Groovy gone it",
    "only counted lambdas and record accessors — it failed a build for adding a `record`. It is",
    "now an observation. A ratchet on a metric that grows with correct work gets switched off.",
    "",
    "Checkstyle is NOT ratcheted — its 9 violations were all unused imports and were fixed",
    "outright, so it is a hard zero-tolerance gate.",
    "",
    "To lower a ceiling: fix violations, run",
    "  ./mvnw -Pquality verify -DskipTests -Djacoco.skip=true \\",
    "      -Dspotbugs.fail=false -Dcheckstyle.fail=false -Dpmd.violation.buildFailOnViolation=false",
    "then set the new counts here in the same commit. Raising a ceiling requires a written",
    "justification in the commit message and CODEOWNERS approval."
  ],
  "recorded": "2026-08-19",
  "_scope_2026_08_19_documented_snippets": [
    "PMD CEILING LOWERED 322 -> 318. EARNED IN THIS PASS AND ATTRIBUTED PER RULE, which is what",
    "the previous entry could not do and said so.",
    "",
    "  AvoidLiteralsInIfCondition  65 -> 60   the five MapFlattener.Builder bound guards",
    "                                        (maxDepth, maxArraySize, maxMapSize, maxArrayCells,",
    "                                        maxJsonStringLength) each wrote `< 1` as a literal.",
    "                                        They are one rule expressed five times; a named",
    "                                        MIN_BOUND constant expresses it once. The 65 is the",
    "                                        intermediate figure AFTER this pass added a sixth",
    "                                        such guard with maxArrayCells - the point went up",
    "                                        before it came down, and it was paid rather than",
    "                                        banked against the four points of headroom.",
    "  ReturnEmptyCollectionRatherThanNull 17 -> 16",
    "                                        MapFlattener.asNestedList returned List<?> or null.",
    "                                        Split into a predicate plus an accessor: the rule is",
    "                                        right that a collection-or-null return makes every",
    "                                        caller a NullPointerException candidate.",
    "",
    "  Walk: 322 (recorded) -> 324 (this pass's first draft) -> 318 (final).",
    "",
    "SPOTBUGS UNMOVED AT 231, AND THE MARGIN WAS PAID NOT BANKED. An intermediate measurement read",
    "232: MapFlattener.chargeCells logged the refusal at WARN with the caller's column key in the",
    "message, and find-sec-bugs counts that as CRLF_INJECTION_LOGS. The complaint was right about",
    "the wrong half. The refusal already reaches the caller in full as the exception message, so",
    "the log line was DOUBLE REPORTING whose only distinctive effect was one line per refusal on",
    "the exact input class an attacker controls. It is deleted rather than sanitised.",
    "CRLF_INJECTION_LOGS 88 -> 87.",
    "",
    "CHECKSTYLE UNMOVED AT 0.",
    "",
    "THE PREVIOUS ENTRY'S UNATTRIBUTED POINT IS STILL UNATTRIBUTED, and that is not this pass's",
    "delta. _scope_2026_08_19_review below records PMD measuring 322 against a 323 ceiling with",
    "the single-violation difference unaccounted for, and declines to take it on those grounds.",
    "This pass measured 324 BEFORE its own changes to the builder guards, i.e. 322 plus the two",
    "it added, so the tail figure the previous pass could not name is unchanged and unnamed. The",
    "four points taken here are named ones.",
    "",
    "Test count 2530 -> 2634 (Maven summary line, authoritative; the surefire-XML sum reads 2102",
    "and undercounts by 532 because @Nested classes emit a separate report each while the outer",
    "class reports tests=0). Fidelity corpus 161 -> 164 fixtures, 56/24/81 -> 58/25/81."
  ],
  "_scope_2026_08_19_review": [
    "SPOTBUGS CEILING LOWERED 236 -> 231. THE DROP IS NOT THIS PASS'S WORK AND SAYING SO IS THE",
    "POINT: it belongs to e65fec7, the array-element alignment commit, which did not touch this",
    "file. Measured with the CI invocation on both trees rather than inferred:",
    "",
    "  e65fec7 main sources, this pass's tests: 231  (CRLF_INJECTION_LOGS 86, REC_CATCH_EXCEPTION 13)",
    "  this pass, first draft:                  232  (CRLF_INJECTION_LOGS 87)",
    "  this pass, final:                        231  (CRLF_INJECTION_LOGS 86)",
    "",
    "The +1 was a single LOG.trace in FileFinder.closeQuietly that interpolated",
    "exception.getMessage() into the format string. It was paid for rather than banked against the",
    "five points of headroom e65fec7 left: the format string is now constant and the exception",
    "goes in as the throwable argument, which is strictly more information at the same cost. A",
    "ceiling with slack in it is a ceiling that stops measuring, so the slack is removed in the",
    "same commit that measures it.",
    "",
    "The removed finding is identified, not inferred: FileFinder.java line 1067,",
    "'Logger.trace(String,Object) might be used to include CRLF characters', present in the",
    "323-era run's SpotBugs output and absent from the final one. Nothing else in FileFinder's",
    "finding list changed except line numbers.",
    "",
    "CHECKSTYLE UNMOVED AT 0. PMD MEASURED 322 AGAINST A CEILING OF 323, AND THE CEILING IS",
    "DELIBERATELY NOT LOWERED. The single-violation delta appeared between two CI-invocation runs",
    "whose only source difference is the LOG.trace line above, and it is NOT in FileFinder: all 27",
    "of that file's PMD violations are accounted for and none sits near the edited method. All ten",
    "topRules figures are identical to the recorded ones, so the delta is one violation somewhere",
    "in the 63-entry tail and I could not name it. Lowering a ratchet on an unattributed",
    "measurement is the same mistake as raising one on an unattributed measurement - it records a",
    "number nobody can reproduce a reason for. 322 is under 323, the gate passes, and the next",
    "pass that can name the rule should take the point.",
    "",
    "AN INVOCATION TRAP, WRITTEN DOWN BECAUSE IT HAS NOW MISLED TWO PASSES. Running",
    "`./mvnw -Pquality compile pmd:pmd` instead of the full CI command reports 282 violations over",
    "34 analysed files, not 323. The previous commit's message records the same shape as 'PMD is",
    "283 (not 323)' and treated it as a refutation of the recorded ceiling. It is not - it is a",
    "partial run. ALWAYS use the command in the comment above, the one quality.yml uses, before",
    "comparing anything to a number in this file.",
    "",
    "Test count 2470 -> 2530 (Maven summary line, authoritative; the surefire-XML sum reads 1998",
    "and undercounts by 532). Fidelity corpus unmoved at 161 fixtures, 56/24/81."
  ],
  "_scope_2026_08_18_review": [
    "ADVERSARIAL-REVIEW PASS. CEILINGS UNMOVED AT 0 / 323 / 236, AND THAT IS THE RESULT, NOT AN",
    "ABSENCE OF ONE: this pass adds one log.warn on the array-element union path and both ratchets",
    "went UP by exactly one before the two of them were paid for individually. Measured against a",
    "clean worktree at 1e21e42 rather than against the numbers written below, which is how the",
    "stale figure in the third bullet was found.",
    "",
    "  PMD       323 -> 324 -> 323.",
    "    GuardLogStatement 39 -> 40 -> 39. The new warn had scalar.getClass().getSimpleName() in",
    "    its argument list. Every argument is now hoisted into a local first. The rule is right:",
    "    an unguarded log statement should not do work to build arguments it may discard.",
    "",
    "  SpotBugs  236 -> 237 -> 236.",
    "    CRLF_INJECTION_LOGS 86 -> 87. Not removable: the warn exists to print the caller's value,",
    "    which is what the rule describes. The two data-derived arguments now go through a CR/LF",
    "    stripper anyway - find-sec-bugs still taints them, and the log-forging vector is real",
    "    whether or not the analyser credits the fix.",
    "    SIC_INNER_SHOULD_BE_STATIC 1 -> 0 pays for it, at a site that had been reported all along:",
    "    SchemaBasedMapConverter.AvroUnionConverter was the only one of the six converters in that",
    "    file declared non-static, and its constructor already takes its parent explicitly, so the",
    "    implicit outer reference was never read. A real finding, fixed - not a suppression.",
    "",
    "ONE FIGURE IN note_2026_08_18 BELOW IS WRONG AND IS CORRECTED HERE RATHER THAN OVERWRITTEN.",
    "It records 'CRLF_INJECTION_LOGS 91 -> 92 ROSE by one NET'. Measured at 1e21e42 - the very",
    "commit that note describes - CRLF_INJECTION_LOGS is 86. The total of 236 in that note is",
    "correct and independently reproduced; only the per-rule figure is off, by six. Left in place",
    "with this correction beside it, because silently editing a measurement is how the previous",
    "three false records in this file survived as long as they did.",
    "",
    "THE EMPTY-CATCH POPULATION IS 26, NOT 25, AND NOW IT IS WRITTEN DOWN. _scope_2026_08_18",
    "below reports '25 sites, classified per-site BEFORE being touched, all 25 resolved'. That is",
    "accurate about PMD'S LIST and not about the tree: a comment-stripping regex over src/main at",
    "ccb3dab finds 26 genuinely-empty catch blocks. The delta was",
    "SchemaBasedMapConverter.AvroUnionConverter.doConvert, exempt only because it was written",
    "catch (Exception ignored) and PMD's EmptyCatchBlock allows ^(ignored|expected)$ - the same",
    "naming hatch that note records as REJECTED at 24 of the 25 it did classify. The one site",
    "already using the hatch was never classified, because the gate never reported it. Repaired in",
    "this pass with the ConversionFailure.first pattern. The same regex over src/main now returns",
    "ONE site: JsonReconstructor.parseAtomicValue, catch (NumberFormatException expected), which",
    "is the documented probe where the exception genuinely IS the answer.",
    "",
    "A GATE THAT EXEMPTS BY VARIABLE NAME CANNOT BE THE ONLY COUNT OF A DEFECT CLASS. Re-run the",
    "regex, not the rule, before claiming a class is cleared."
  ],
  "_scope_2026_08_17": [
    "THE FIVE-CLASS SPOTBUGS EXCLUDE BLOCK IS GONE, AND THE FOLLOW-UP IT OWED IS DISCHARGED.",
    "",
    "src/main/spotbugs/spotbugs-exclude.xml carried a five-class x three-pattern <Match> with no",
    "<Method> narrowing, masking ten findings in hand-written Java. All ten are fixed and the",
    "block is deleted rather than narrowed. Nothing about it survived: JsonFlattener and",
    "JsonReconstructor never contributed a single finding to it and were pure collateral.",
    "",
    "MEASURED, and two predictions were WRONG in the same direction - both gates fell further",
    "than the analysis predicted. Stated loudly rather than quietly banked:",
    "",
    "  SpotBugs  241 -> 237.  PREDICTED 241 (no change).",
    "    The prediction was that fixing the ten could not lower the number, because 241 was the",
    "    count WITH the block and 251 the count WITHOUT it - the ten ARE the difference. That",
    "    arithmetic is right, and the number still fell by 3, because THREE findings that were",
    "    always counted inside the 241 lived inside the body of one of the deleted methods:",
    "      DE_MIGHT_IGNORE       AvroReconstructor.calculateArraySize   (the empty catch)",
    "      REC_CATCH_EXCEPTION   AvroReconstructor.calculateArraySize   (same catch)",
    "      SF_SWITCH_NO_DEFAULT  AvroReconstructor.calculateArraySize   (switch on arrayFormat)",
    "    Deleting dead code took its live findings with it. 241 - 3 = 238.",
    "    A FOURTH came off with the BL-010 constructor repair, and it is worth naming because",
    "    the gate had been reporting the bug all along at HIGH priority:",
    "      NP_NULL_ON_SOME_PATH  JsonFlattener.<init>  'Possible null pointer dereference of",
    "                            config ... Dereferenced at line 224, Known null at line 223'",
    "    That is JsonFlattener.with(flattener, null) throwing NPE - the guard on 223 defeated by",
    "    the parameter read on 224. 238 - 1 = 237.",
    "",
    "  PMD       361 -> 351.  PREDICTED 353-356.",
    "    UnusedPrivateMethod       5 -> 0   the five dead methods; PMD has no per-class",
    "                                      exclusions and had been counting them all along",
    "    UnusedFormalParameter    40 -> 38",
    "    EmptyCatchBlock          26 -> 25",
    "    AvoidDuplicateLiterals   25 -> 24",
    "    SwitchStmtsShouldHaveDefault 2 -> 1",
    "",
    "TWO STALE NUMBERS IN THIS FILE, corrected while re-measuring. The pmd.topRules block below",
    "recorded AvoidLiteralsInIfCondition at 58 and EmptyCatchBlock at 25. Measured at the",
    "PRE-CHANGE baseline they were 62 and 26. Those entries were already wrong before this work",
    "and are now re-measured rather than carried forward.",
    "",
    "Checkstyle unmoved at 0. Test count 2333 -> 2389 (Maven summary line, authoritative;",
    "the surefire-XML sum reads 1840 and undercounts by 532 because @Nested classes emit a",
    "separate report each while the outer class reports tests=0)."
  ],
  "_scope_2026_08_11": [
    "WHY THE GROOVY REMOVAL LOWERED NOTHING HERE, measured rather than assumed.",
    "",
    "The removal deleted 7,778 lines of Groovy, all of it under src/test. It was predicted to",
    "move SpotBugs down. It moved nothing, and the reason is that none of the three analysers",
    "can see test code at all:",
    "",
    "  Checkstyle  <includeTestSourceDirectory>false</includeTestSourceDirectory>  (pom.xml)",
    "  PMD         <includeTests>false</includeTests>                              (pom.xml)",
    "  SpotBugs    target/spotbugsXml.xml records exactly one analysed <Jar>:",
    "              target/classes. No test tree, no test classes.",
    "",
    "So the deletion was invisible to all three by construction, and equally the ~2,700 lines of",
    "Java the port ADDED under src/test could not have raised them either. Counts re-measured",
    "after the removal: Checkstyle 0, PMD 361, SpotBugs 241 — all three identical to the",
    "pre-removal ceilings, which is the expected result once the scoping above is known.",
    "",
    "Recorded so the next reader does not spend the same hour rediscovering that a src/test",
    "change cannot move a src/main metric."
  ],
  "checkstyle": {
    "ceiling": 0,
    "mode": "zero-tolerance",
    "note": "All 9 original violations were unused imports and are fixed. Re-measured 0 on 2026-08-11 after the Groovy removal. Note this gate never saw the ported test sources: includeTestSourceDirectory is false, so the default-package AvroReconstructorTest that was moved into io.github.pierce during the port was moved for Java-tooling reasons, not because Checkstyle would have rejected it — Checkstyle was never going to look."
  },
  "pmd": {
    "ceiling": 318,
    "mode": "ratchet",
    "topRules": {
      "AvoidLiteralsInIfCondition": 60,
      "GuardLogStatement": 39,
      "NullAssignment": 38,
      "UnusedFormalParameter": 37,
      "AvoidDuplicateLiterals": 24,
      "ReturnEmptyCollectionRatherThanNull": 16,
      "UseLocaleWithCaseConversions": 14,
      "SystemPrintln": 14,
      "LiteralsFirstInComparisons": 13,
      "EmptyCatchBlock": 0
    },
    "note": "322 -> 318 on 2026-08-19; see _scope_2026_08_19_documented_snippets for the per-rule attribution and for why the point went UP to 324 before it came down. 351 -> 323 on 2026-08-18, driven by EmptyCatchBlock 25 -> 0 and GuardLogStatement 44 -> 39. See _scope_2026_08_18 for why a commented catch does not satisfy the rule as configured, and why the two obvious one-line ways to clear it were rejected. EmptyCatchBlock is now listed at 0 in topRules deliberately: it is the entry a future reader will want, and a zero that is written down is harder to let drift back. 361 -> 351 on 2026-08-17, all of it from deleting the five dead private methods the SpotBugs exclude block used to hide. This is where the ratchet actually moved: pmd-ruleset.xml pulls in category/java/bestpractices.xml whole, which contains UnusedPrivateMethod, and PMD has no per-class exclusions at all, so it had been counting all five inside its total the entire time the SpotBugs block was hiding them. Rules that fell: UnusedPrivateMethod 5 -> 0, UnusedFormalParameter 40 -> 38, EmptyCatchBlock 26 -> 25, AvoidDuplicateLiterals 25 -> 24, SwitchStmtsShouldHaveDefault 2 -> 1. The topRules figures above are re-measured; the previous entry recorded AvoidLiteralsInIfCondition as 58 and EmptyCatchBlock as 25 when the true pre-change values were 62 and 26. EARLIER: 449 -> 361 on 2026-08-10, AvoidFieldNameMatchingMethodName (88) excluded project-wide in pmd-ruleset.xml because it fires on the record-style accessor convention the language itself generates. EmptyCatchBlock (25) is still the one to attack next — it is the same swallowed-exception class the audit flagged as laundering failures into apparent successes."
  },
  "spotbugs": {
    "ceiling": 231,
    "mode": "ratchet",
    "note_2026_08_17": "FOLLOW-UP DISCHARGED, block deleted, 241 -> 237. The five-class x three-pattern exclude <Match> is gone from src/main/spotbugs/spotbugs-exclude.xml and all ten findings it masked are fixed. TWO CORRECTIONS TO THE FIRST VERSION OF THIS NOTE, both made after measuring rather than re-reading it. (1) THE COUNT. This note first said '241 -> 238' and 'THE COUNT FELL BY 3, NOT BY 0 AS PREDICTED'. Both numbers were wrong and both contradicted the ceiling stored a few lines above them in this same object. Measured with the CI command (grep -o '<BugInstance ' target/spotbugsXml.xml | wc -l): 237. The walk is 241 with the block, 251 without it, 237 after the fixes - it fell by FOUR. Three of the four lived in the body of calculateArraySize and went with the deletion (DE_MIGHT_IGNORE and REC_CATCH_EXCEPTION on its empty catch, SF_SWITCH_NO_DEFAULT on its default-less switch); the fourth is the NP_NULL_ON_SOME_PATH on the JsonFlattener constructor, repaired in the same commit and carried in its own changelog bullet. (2) THE unwrapUnion VERDICT, restated below. VERDICT ON THE FIVE DEAD PRIVATE METHODS, each established from the occurrence count of the symbol in every historical revision of its file following renames - not from reading the code and guessing: THREE WERE BORN DEAD, never called in any revision - AvroReconstructor.reconstructNestedArrayOfRecords (count 0 before cad816b, 1 in cad816b and every revision since; the live variant reconstructNestedArrayOfRecordsAtIndex was added by the SAME commit), MapFlattener.flattenSingleValue (count 1 in all 8 revisions of the file), and AvroReconstructor.unwrapUnion - which this note previously mis-filed as 'caller replaced'. unwrapUnion is born dead in the STRONGEST sense of the three: through ef625f2 the file contains FOUR occurrences of the symbol and all four are CALLS, with no declaration in any revision where a call exists; the declaration first appears at cad816b, the very commit that deleted all four calls, and never acquired a caller. The class has no superclass and the repo-wide count equals the per-file count at every revision, so it was not inherited; the file was Groovy-compiled then, so the four calls compiled and would have thrown MissingMethodException if reached. The claim 'its 3 call sites became unwrapNullable' was false twice over - there were four, and unwrapNullable's own count rose only 7 -> 8 at cad816b, so at most one site was replaced one-for-one while the branch structure was rewritten wholesale. TWO HAD CALLERS THAT A SPECIFIC COMMIT REMOVED, and in both the commit installed a named replacement at the same decision point, so nothing stopped running and no behaviour was owed restoration - AvroReconstructor.calculateArraySize (declaration at ef625f2:2018 plus one call at ef625f2:1192, which became determineArraySize, introduced by the same commit) and GAvroSchemaFlattener.isNullable(Schema), whose caller was still LIVE at 5df36e2:317 and at 1ba32af:317 and became a comment at 4a49041 - NOT at 5df36e2 as this note first said. The 4a49041 diff shows the replacement was deliberate: it adds a nullable field to SchemaNode, threads it through the constructor, and swaps the local isNullable(currentSchema) call for node.nullable. NO caller was lost, so NO corpus DEFECT is explained by any of the five, and the fidelity corpus stayed green through the change - 156 fixtures, 51/23/82 unmoved, verified rather than assumed. ONE LOSSY SUBSTITUTION IS NOW UNDOCUMENTED IN CODE and is tracked in docs/BACKLOG.md instead: determineArraySize has no BRACKET_LIST/COMMA_SEPARATED/PIPE_SEPARATED fallback where calculateArraySize did. The unwrapNullable arity-3+ gap tracked as BL-014 is NOT a lost behaviour - since unwrapUnion never executed, no arity-3+ handling was ever in service, so that entry is a never-implemented gap in the array-element path rather than a regression. CORRECTION TO THE PREVIOUS NOTE'S WORDING: it said 'deleting it would RAISE the ceiling, which is forbidden'. Deleting the block raises the measured COUNT to 251; the ceiling is a recorded number that only ever moves by hand. The ratchet would have rejected 251 against the 241 ceiling - that is the accurate statement, and it is why the ten had to be fixed in the same change as the deletion.",
    "note_2026_08_11": "SUPERSEDED by note_2026_08_17, retained for the measurement it records. UNCHANGED at 241 by the Groovy-toolchain removal, and that is itself the finding. RE-MEASURED INDEPENDENTLY on 2026-08-11 during adversarial-review follow-up, because the review task asserted the exclude block 'named five now-deleted classes' and that SpotBugs should therefore fall. Both halves are false. (1) All five classes are present as ordinary Java: src/main/java/io/github/pierce/{AvroReconstructor,GAvroSchemaFlattener,JsonFlattener,JsonReconstructor,MapFlattener}.java. None was deleted, and src/main went Groovy-free well before this change, so nothing about porting src/test/groovy could have retired the block. Its old 'Groovy-compiled classes' label was already false when written. (2) Deleting the block and re-running spotbugs:spotbugs gives 251 <BugInstance> entries against 241 with it - 10 masked findings in hand-written Java, none a compiler artefact: UPM_UNCALLED_PRIVATE_METHOD x5, SIC_INNER_SHOULD_BE_STATIC_ANON x3, RCN_REDUNDANT_NULLCHECK_OF_NONNULL_VALUE x2. No SpotBugs ceiling reduction was earned by the Groovy removal.",
    "note": "242 -> 241 on 2026-08-10: the io.github.pierce.schema package added 6 findings and now contributes 0. Four were fixed (SE_BAD_FIELD x2 - FlattenOptions declared Serializable while holding non-serializable TypeMapper and LeafInterceptor fields, so it could not serialize at all, not even with defaults; UC_USELESS_OBJECT; SE_NO_SERIALVERSIONID). Two EI_EXPOSE_REP on FlattenedField are suppressed by named method in spotbugs-exclude.xml with reasoning. Still includes EI_EXPOSE_REP on the Spark result types and SE_BAD_FIELD on NexusPiercerSparkPipeline, which corroborates the audit's finding that a non-transient SparkSession is captured into task serialization.",
    "note_2026_08_18": "237 -> 236, and the single-finding margin is the whole story. This pass adds diagnostic logging on paths that used to fail in silence, and find-sec-bugs counts every log statement that echoes caller-supplied text as CRLF_INJECTION_LOGS - so an intermediate measurement read 250, thirteen ABOVE the ceiling, before any of it was paid for. It was paid for by narrowing six catch(Exception) blocks to the exception actually thrown (REC_CATCH_EXCEPTION 20 -> 14), by the FIXED-decode empty catches taking DE_MIGHT_IGNORE 1 -> 0, and by folding five near-identical union-miss debug statements into one helper. Recorded because the shape of this is going to recur: a gate that counts log statements is in tension with a pass whose purpose is to make silent failures audible, and the tension was resolved by making the surrounding code genuinely better rather than by raising the number."
  },
  "_scope_2026_08_18": [
    "THE 25 EmptyCatchBlock FINDINGS ARE GONE, AND SO IS THE ADVICE THAT WOULD HAVE FAKED IT.",
    "",
    "The 2026-08-17 note below said EmptyCatchBlock (25) 'is still the one to attack next - it is",
    "the same swallowed-exception class the audit flagged as laundering failures into apparent",
    "successes.' It was attacked. It is 0. But the obvious route was measured first and rejected:",
    "",
    "  PMD'S RULE DOES NOT ACCEPT A COMMENTED CATCH, AS CONFIGURED. The rule body in",
    "  category/java/errorprone.xml (unzipped from pmd-java-6.55.0.jar) gates its XPath on",
    "  [$allowCommentedBlocks != true()] and declares the property value=\"false\";",
    "  src/main/pmd/pmd-ruleset.xml pulls errorprone.xml in wholesale and sets no properties.",
    "  ALL 25 SITES ALREADY CARRIED COMMENTS - 'Continue trying other formats', 'Not valid",
    "  Base64', 'Fall through to other formats' - and all 25 still counted. Adding a better",
    "  comment would have resolved exactly zero. Flipping allowCommentedBlocks to true would have",
    "  cleared all 25 in one line with no thought applied to any of them, and the comments it",
    "  would bless include nine occurrences of '// Continue'. That is a ratchet ceiling raised by",
    "  other means. Renaming all 25 variables to 'ignored' - the rule's own escape hatch - was",
    "  rejected for 24 of them on the same grounds: at 21 sites the exception is not ignored, it",
    "  is the signal that advances the cascade. The hatch is used at exactly ONE site,",
    "  JsonReconstructor.parseAtomicValue, where a NumberFormatException genuinely IS the answer.",
    "",
    "PER-SITE OUTCOMES, classified before being touched:",
    "  (a) 18 FORMAT-CASCADE sites in the four date/time converters. Each catch now records the",
    "      FIRST failure through ConversionFailure.first, and each terminal names the formats it",
    "      tried and carries that cause. Two intermediate shapes were measured and discarded: an",
    "      unconditional 'lastFailure = e' cost 18 PMD UnusedAssignment (correctly - each is",
    "      overwritten unread), and an inline 'if (firstFailure == null)' cost 12 SpotBugs",
    "      NP_LOAD_OF_KNOWN_NULL_VALUE / RCN_REDUNDANT_NULLCHECK_* instead.",
    "  (b) 3 FIXED-decode sites in AvroReconstructor. Each now records why that strategy declined,",
    "      into a bitmask rather than a StringBuilder because this is a hot path. Two of the five",
    "      strategies decline WITHOUT throwing and left no trace at all; those are recorded too,",
    "      and the raw-platform-bytes fallback that they lead to now warns when it silently",
    "      succeeds.",
    "  (c) 2 in JsonReconstructor and 1 in FileFinder. The JsonReconstructor array site is a",
    "      HYBRID - a legitimate cascade under three formats and a genuine laundering under the",
    "      JSON default, where the switch falls through and returns the raw text as a one-element",
    "      array - so only the JSON arm reports. FileFinder's catch was correct and its COMMENT",
    "      was not: it claimed 'size is checked on open instead' and there is no size check on",
    "      open anywhere in the file. That false compensating-control claim is now corrected in",
    "      place and filed as files/NP-027.",
    "",
    "MEASURED, AND THE PREDICTION WAS WRONG IN BOTH DIRECTIONS BEFORE IT WAS RIGHT.",
    "",
    "  PMD       351 -> 323.  Predicted 326, honest band 326-331.",
    "    EmptyCatchBlock          25 -> 0",
    "    GuardLogStatement        44 -> 39   NOT predicted. This pass ADDS logging, which should",
    "                                       have raised it, and an intermediate measurement did:",
    "                                       360 total with GuardLogStatement at 52. Every new log",
    "                                       call that had a method invocation in its argument list",
    "                                       was rewritten to hoist it, and five near-identical",
    "                                       union-miss debug lines were folded into one helper.",
    "    UnusedAssignment          9 -> 9   after the firstFailure rework; it was 27 before.",
    "    UnusedFormalParameter    38 -> 37",
    "    AvoidLiteralsInIfCondition 62 -> 64  ROSE. The new format-mismatch and cardinality checks",
    "                                       compare against literals.",
    "    AvoidDuplicateLiterals   24 -> 25   ROSE, by one, in AvroReconstructor.",
    "",
    "  SpotBugs  237 -> 236.  Predicted 'may also fall; do not bank it.' It nearly did not.",
    "    An intermediate measurement read 250 - THIRTEEN ABOVE THE CEILING - because every new",
    "    log.warn that echoes caller-supplied text is a CRLF_INJECTION_LOGS finding and this pass",
    "    exists to add log.warn calls. Getting back under it was real work, not bookkeeping:",
    "    REC_CATCH_EXCEPTION      20 -> 14   six catch(Exception) narrowed to the type actually",
    "                                       thrown - JsonProcessingException over a String parse,",
    "                                       IllegalArgumentException from Base64, RuntimeException",
    "                                       where nothing checked can be thrown at all",
    "    DE_MIGHT_IGNORE           1 -> 0   the FIXED-decode empty catches",
    "    CRLF_INJECTION_LOGS      91 -> 92  ROSE by one NET, after folding five union-miss log",
    "                                       sites into one and dropping four marginal new ones",
    "  The margin is ONE finding. The next agent to add a log line that echoes user data will",
    "  break this gate, and that is the correct outcome rather than an inconvenience.",
    "",
    "  Checkstyle unmoved at 0. Test count 2389 -> 2470 (Maven summary line, authoritative; the",
    "  surefire-XML sum reads 1929 and undercounts by 541 because @Nested classes emit a separate",
    "  report each while the outer class reports tests=0).",
    "",
    "  Fidelity corpus 156 -> 161 fixtures, 51/23/82 -> 55/24/82. Three rows moved and five were",
    "  added; see CHANGELOG.md and src/test/resources/fidelity/manifest.json."
  ]
}
