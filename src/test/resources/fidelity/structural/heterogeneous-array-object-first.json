{
  "id" : "heterogeneous-array-object-first",
  "family" : "structural",
  "title" : "Heterogeneous array, objects before the scalars - object elements silently deleted",
  "stack" : "BOTH",
  "referenceIssue" : "",
  "rationale" : "The same shape as the previous fixture with element order reversed, and the damage is strictly worse: instead of a visible sentinel the object elements vanish and the output is a clean, plausible, wrong array. It exists solely because LinkedHashMap iteration order decides which of two colliding keys is written last.",
  "catchesBugClass" : "Order-dependent silent data destruction from leaf/branch key collision. Also catches any change to MapFlattener fieldValues insertion order, which would flip this fixture to the sentinel-leak failure mode instead.",
  "cannotCatch" : "It cannot detect the loss from the output alone - that is the point, and it means the harness must compare against the input, never self-validate the result. It also does not prove heterogeneous arrays fail in general, only in this collision shape.",
  "predicted" : {
    "classification" : "DEFECT",
    "detail" : "The one-segment key still overwrites the array holder built from the longer keys, so both object elements are still deleted and this row stays DEFECT. Since 2.1.0 the result is no longer 'syntactically perfect': the array comes back at its true length of four as [null, null, x, y], so the two deleted objects leave explicit holes at the indices they occupied instead of vanishing into a clean two-element string array that reads as valid data. The deletion is unchanged; its detectability is not."
  },
  "configDescription" : "defaults",
  "config" : { },
  "input" : "{\"mixed\":[{\"a\":1},{\"a\":2},\"x\",\"y\"]}",
  "expected" : {
    "mapBaseline" : "{\"mixed\":[{\"a\":\"I:1\"},{\"a\":\"I:2\"},\"S:x\",\"S:y\"]}",
    "flat" : "{\"mixed\":\"S:[null,null,\\\"x\\\",\\\"y\\\"]\",\"mixed_a\":\"S:[1,2,null,null]\"}",
    "mapDoc" : "THROWN: KeyCollisionException: leaf/branch key collision: the key 'mixed' is also an intermediate path of [mixed_a], so the reconstructed node at 'mixed' would have to be a scalar and an object at once. If any of [mixed_a] was meant as a literal field name containing the",
    "losslessMap" : false,
    "mapDefaultsMatch" : true,
    "jsonBaseline" : "{\"mixed\":[{\"a\":\"I:1\"},{\"a\":\"I:2\"},\"S:x\",\"S:y\"]}",
    "flatJson" : "{\"mixed\":\"S:[null,null,\\\"x\\\",\\\"y\\\"]\",\"mixed_a\":\"S:[1,2,null,null]\"}",
    "jsonDoc" : "THROWN: KeyCollisionException: leaf/branch key collision: the key 'mixed' is also an intermediate path of [mixed_a], so the reconstructed node at 'mixed' would have to be a scalar and an object at once. If any of [mixed_a] was meant as a literal field name containing the",
    "losslessJson" : false,
    "jsonDefaultsMatch" : true
  },
  "measuredLossless" : false
}
