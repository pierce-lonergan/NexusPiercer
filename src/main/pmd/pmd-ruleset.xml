<?xml version="1.0"?>
<ruleset name="Custom Rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0
        https://pmd.sourceforge.io/ruleset_2_0_0.xsd">
    <description>Custom PMD rules</description>

    <rule ref="category/java/bestpractices.xml"/>

    <!--
      AvoidFieldNameMatchingMethodName is excluded project-wide.

      It fires on the record-style accessor convention — a `separator` field with a `separator()`
      reader — which is exactly what Java's own `record` keyword generates, and what
      io.github.pierce.schema uses throughout. Honouring it would mean renaming every accessor to
      getX() to satisfy a naming rule the language itself violates.

      Excluded here rather than suppressed per file so the reasoning lives in one place. It
      accounts for the large majority of both the pre-existing violations and those added by the
      schema package; the ratchet ceiling drops to match, so this narrows tolerated debt rather
      than granting an amnesty.
    -->
    <rule ref="category/java/errorprone.xml">
        <exclude name="AvoidFieldNameMatchingMethodName"/>
    </rule>
</ruleset>
